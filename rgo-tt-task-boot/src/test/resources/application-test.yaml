spring:
  main:
    web-application-type: none
    banner-mode: off

cors:
  origins: 'http://localhost:5173'
  methods: GET, POST, PUT, DELETE
  maxAgeSeconds: 3600

armeria:
  ports:
    - port: 8080
      protocols:
        - HTTP